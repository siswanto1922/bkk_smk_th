<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moemoedev.bkksmkthp">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.AnnouncementActivity"></activity>
        <activity android:name=".Activity.AnnouncementUpload" />
        <activity android:name=".Activity.ApplicantDetailActivity" />
        <activity android:name=".Activity.CheckApplicantActivity" />
        <activity android:name=".Activity.DetailLokerActivity" />
        <activity android:name=".Activity.UploadLokerActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>